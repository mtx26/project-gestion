{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    // `process.env` est lu ici (inline par les bundlers au build).
    "types": ["node"]
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules"]
}
