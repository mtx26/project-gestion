# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Pinned to SDK 54 (not the latest SDK) because the only device available for on-device Expo Go
testing is capped at SDK 54 by its OS version. Do not bump the `expo` package or its managed
dependencies past the SDK 54 range without confirming the test device situation has changed.
